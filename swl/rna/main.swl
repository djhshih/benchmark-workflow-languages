# RNA-seq Pipeline in SWL

# Import tool definitions
trim = import "trimmomatic.sh"
align = import "star.sh"
fastqc = import "fastqc.sh"
feature_counts = import "featurecounts.sh"

# Workflow: trim -> align -> fastqc/featurecounts (parallel)
trim | align | (fastqc & feature_counts)